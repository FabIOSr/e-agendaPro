{
  "name": "agendapro",
  "version": "1.0.0",
  "description": "AgendaPro - Sistema de agendamento para profissionais",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "npm run build && npx serve -l 3000 dist",
    "test": "node tests/run-tests.js"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  }
}
